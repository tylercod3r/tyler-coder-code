import { Prompt, Montserrat } from "@next/font/google";

// config font
export const HeaderFont = Prompt({
    weight: "400",
    subsets: ["latin"],
  });
  
export const SubHeaderFont = Montserrat({
    weight: "400",
    subsets: ["latin"],
  });
